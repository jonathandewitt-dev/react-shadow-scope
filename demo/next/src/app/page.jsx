import { Demo } from '../../../demo';
// import * as reactShadowScope from 'react-shadow-scope'
import * as reactShadowScope from '../../../../dist';

const Home = () => <Demo reactShadowScope={reactShadowScope}/>;

export default Home;
